# 新电脑环境搭建指南

> 从零开始在新电脑上搭建 OA 项目开发环境

---

## 一、安装软件

### 必装

| 软件 | 版本 | 下载 | 备注 |
|------|------|------|------|
| **Node.js** | 18+ LTS | https://nodejs.org | 安装后确认 `node -v` |
| **Git** | 最新 | https://git-scm.com | 安装后配置用户名和邮箱 |
| **微信开发者工具** | 最新稳定版 | https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html | 小程序编译调试 |
| **VS Code**（推荐） | 最新 | https://code.visualstudio.com | 代码编辑 |

### 选装

| 软件 | 用途 |
|------|------|
| **MySQL Workbench** 或 **DBeaver** | 数据库管理 |
| **Redis Insight** | Redis 缓存管理 |
| **Postman** 或 **Apifox** | API 调试 |
| **Git Bash** | Windows 下使用 Linux 命令（Git 自带） |

### 安装后配置 Git

```bash
git config --global user.name "殇血轮回"
git config --global user.email "你的邮箱"
```

---

## 二、拷贝密钥文件

从旧电脑拷贝以下文件到新电脑**同路径**：

| 文件 | 旧电脑路径 | 用途 |
|------|-----------|------|
| `wx_app_key.pem` | `C:\Users\yf\.ssh\` | 服务器 SSH 私钥 |
| `config` | `C:\Users\yf\.ssh\` | SSH 连接配置 |
| `known_hosts` | `C:\Users\yf\.ssh\` | 服务器指纹 |
| `.env` | `Y:\AI\WX-APP-OA\backend\.env` | 后端全部环境变量 |

> ⚠️ 如果新电脑用户名不同，`.ssh` 路径对应调整。SSH 私钥需保持 `600` 权限。

---

## 三、克隆项目

```bash
# 在 Y 盘创建项目目录
mkdir Y:\AI\WX-APP-OA
cd Y:\AI\WX-APP-OA

# 克隆
git clone https://github.com/War-Blood/WX_APP_OA.git .
git checkout test
```

---

## 四、安装依赖

```bash
cd backend  && npm install
cd ../webapp  && npm install
cd ../miniapp && npm install
```

---

## 五、放置 .env 文件

```bash
# 将复制的 .env 放到 backend 目录
cp /path/to/copied/.env Y:\AI\WX-APP-OA\backend\.env
```

---

## 六、验证环境

### 1. SSH 连接

```bash
ssh -i C:\Users\yf\.ssh\wx_app_key.pem root@111.229.107.123 echo OK
```

### 2. 后端启动

```bash
cd Y:\AI\WX-APP-OA\backend
npm run dev
# 访问 http://localhost:3000/api/health 返回 {"status":"ok"}
```

### 3. Webapp 构建

```bash
cd Y:\AI\WX-APP-OA\webapp
npm run build
# 输出 dist/ 目录即成功
```

### 4. 小程序编译

```bash
cd Y:\AI\WX-APP-OA\miniapp
npm run build:mp-weixin
# 输出 dist/build/mp-weixin/ → 微信开发者工具导入
```

---

## 七、在新电脑上打开项目后输入以下提示词

```
我已完成电脑迁移，项目在 Y:\AI\WX-APP-OA，分支为 test。
请帮我确认以下内容：

1. 检查后端能否启动（npm run dev）
2. Webapp 能否正常构建
3. 小程序能否正常编译
4. 确认 SSH 连接服务器是否正常
5. 确认当前 git 状态和最近的提交记录
6. 列出考试模块的部署状态（哪些已部署到生产、哪些待部署）
7. 后续需要继续开发的功能清单
```

---

## 八、项目结构速查

```
Y:\AI\WX-APP-OA/
├── backend/           ← Node.js API 服务
│   └── src/features/
│       ├── attendance/  ← 考勤模块（已部署）
│       └── exam/        ← 考试模块（已部署后端，待建表）
├── webapp/            ← Vue3 Web 管理后台
│   └── src/views/
│       ├── attendance/  ← 排班/请假/汇总
│       └── exam/        ← 题库/试卷/记录/统计
├── miniapp/           ← uni-app 微信小程序
│   └── src/pages/
│       ├── attendance/  ← 排班/请假/出差
│       └── exam/        ← 练习/考试/成绩
├── sql/               ← 数据库脚本
│   └── v3.0_exam.sql   ← 考试模块建表（待执行）
├── 大纲/
│   ├── PRD/考试模块/    ← 完整 PRD 文档
│   ├── 需求修改/        ← 需求迭代记录
│   └── 设计规范/        ← 设计规范（7 份）
└── 需求/任务日志/       ← 每日工作日志
```

---

## 九、常用部署命令

```bash
# 部署后端
scp -i C:\Users\yf\.ssh\wx_app_key.pem backend/src/xxx.js root@111.229.107.123:/var/www/wx-app-oa/backend/src/xxx.js
ssh -i C:\Users\yf\.ssh\wx_app_key.pem root@111.229.107.123 "pm2 restart wx-app-oa-api"

# 部署 Webapp（⚠️ 用 dist/. 不是 dist/）
cd webapp && npm run build
scp -i C:\Users\yf\.ssh\wx_app_key.pem -r dist/. root@111.229.107.123:/var/www/wx-app-oa/admin/

# 部署小程序（微信开发者工具上传）
cd miniapp && npm run build:mp-weixin
```

---

# 项目通读记录与待确认疑问

> 通读日期:2026-08-03 | 通读者:Claude Code
> 已通读:工作日志 13 份、架构审查报告、考勤缺陷/验收报告、考试模块 PRD 全套、三端源码核对

## 一、项目进度概述

核心 OA 业务(公出日志/考勤/统计/组织/审批/提醒/合规)已上线生产。**考试模块**为最近开发(07-15/16):三端代码已生成、后端已部署,但数据库表未建、小程序未上传、联调未做,尚不可用。最近提交(07-17/18)为新电脑环境迁移文档,之后无新功能开发。

## 二、待确认疑问

### A. 方向与优先级

1. 当前处于环境迁移阶段,核心业务已上线。**接下来最优先推进哪一项?**(考试模块联调上线 / 架构审查 P0 整改 / 新需求)

### B. 考试模块

2. **建表数量不一致**:需求文档与日志写"5 张表",但 `sql/v3.0_exam.sql` 实际只有 **4 张表**(categories/questions/papers/records),`architecture-blueprint.md:50` 也写"5表"。以哪个为准?是否缺一张表需补充?
3. 考试建表脚本是否已在生产数据库执行?还是仍待执行?
4. 考试模块是否按 PRD 里程碑(M0 建表→M8 联调验收)继续完成?联调测试、小程序上传是否排期?

### C. 架构审查整改(2026-07-06 报告 35 项)

5. P0 项大多未修复(daily_reports 索引、认证中间件每请求查 DB、getWorkerStats 全表扫描、小程序无分包、前后端零共享、Redis 无密码)。**是否排期?优先级?**
6. JWT 代码兜底仍为 `'3650d'`(仅 .env 设 7d)——是否同步修改代码兜底?
7. Redis 无密码、docker-compose 弱默认密码——生产是否已加固?

### D. 考勤模块遗留

8. BUG-01 修复后历史 sick/personal 数据被交换存储,`sql/` 下无迁移脚本——**是否需要数据迁移?**
9. BUG-05 时区:已引入 beijingDate,但仍残留 `toISOString().slice(0,10)`(如 schedule.service.js:174 会退一天)——是否修复?
10. BUG-06/07/08(出差并发锁 / 消息通知静默吞错 / 我的排班未融合公出日志)是否处理?
11. 考勤 Bug 修复验证清单"验证结果汇总"表全空——**是否需要回归验证并填写?**

### E. 未实现模块(有文档无代码)

12. **员工状态管理模块**(设计 v2.0,2026-06-26)完全未实现——是否要做?
13. **OKR 模块**(OKR-PRD.md 存在,但无 `v2.5_okr.sql`、无代码)——是否要做?
14. **模块管理** PRD 标注"待实装验证"(后端部署/工号自动生成/侧边栏动态渲染/模块开关控制 API)——是否继续?

### F. 当前工作区未提交改动

15. 工作区有未提交改动:report-edit 今日工作小结校验修复、report-history 状态筛选扩展、版本号 v1.0.2.4→v1.0.2.6、ecosystem.local.config.js 路径、1.md 路径(WarBlood→yf)。**这些是待提交的正式改动还是临时调整?是否需要提交**(按规则 commit 后自动 push test)?
16. `miniapp/.syntax_check_*.js` 未跟踪临时文件、`.memory/captures/` 是否清理?

### G. 其他待办

17. 小程序**分包加载优化**(架构 P0-5,日志多次提及)——是否排期?
18. 微信订阅消息**真实推送测试**(需小程序端授权)——是否已做?
19. 3 处功能 TODO(BizTripManage.loadUsers 空实现 / exam papers.viewPaper 空函数 / exam result 逐题详情)与 `report.service.js:496` 临时调试代码——是否处理?
