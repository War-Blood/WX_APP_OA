# Web 端交互体验与遗漏问题待办清单

> 生成日期：2026-08-04
> 范围：`webapp/` 全局框架、已上线页面、PRD 中尚未实现的 Web 端功能
> 本轮交付：仅生成待办清单，不修改 `webapp/src` 业务代码

---

## 1. 清单摘要

| 优先级 | 数量 | 说明 |
|--------|:----:|------|
| P0 | 7 | 当前会阻断管理员完成核心操作或造成错误行为的缺陷 |
| P1 | 20 | 已上线模块的交互体验、完整性缺口 |
| P2 | 7 | PRD 未实现模块、重构与文档同步 |
| 合计 | 34 | 按模块分组，可直接拆为执行任务 |

### 优先级定义

- **P0**：必须优先修复；用户点击后无反应、页面 404、越权访问、关键表单无法提交。
- **P1**：本迭代建议修复；功能可用但体验断裂、信息不完整、容易误操作。
- **P2**：可排期处理；未开发模块、代码质量、性能与文档一致性。

### 最近执行记录

- 2026-08-04：完成 16 项，部分完成 2 项；使用 Playwright 对登录页、仪表盘、403、个人中心、考勤、出差弹窗和全局搜索完成 13 项浏览器验收，全部通过。
- 2026-08-04（第二批）：补审批分页/搜索/详情/处理、题库选项编辑器、月度人员远程搜索、系统设置校验、用户表单校验、日报批量审核入口和退出确认；使用 Playwright 完成 18 项浏览器验收，全部通过。
- 2026-08-04（第三批）：补齐角色成员管理、角色分组创建/更新链路，并修复角色列表未返回 groupId 的问题；使用 Playwright 完成 8 项浏览器验收，全部通过。
- 2026-08-04（第四批）：用户管理补详情、重置密码、批量启用/禁用/删除、JSON/CSV 导入、CSV 导出和模板下载；使用 Playwright 完成 10 项浏览器验收，全部通过。
- 2026-08-04（第五批）：项目页从“日报聚合”重构为真实项目列表 + 详情，接入 `/api/project/list` 与 `/api/project/detail`；使用 Playwright 完成 6 项浏览器验收，全部通过。
- 2026-08-04（第六批）：审批配置新增“新增审批类型”入口，后端补齐创建接口；使用 Playwright 完成 2 项浏览器验收，全部通过。考试模块按用户要求暂缓验收。
- 2026-08-04（第七批）：Vite 增加 vendor 分包，分离 Vue 体系、Element Plus、ECharts、xlsx 与通用 vendor；使用 Playwright 完成 5 项分包冒烟验收，全部通过。
- 2026-08-04（第八批）：同步 Wiki/README 中失效的 `AppHeader/AppSidebar` 组件引用为当前 `TopBar/ModuleSidebar`，文档引用已与仓库一致。
- 2026-08-04（第九批）：新增前端模块状态层，模块管理保存后可通过本地覆盖实时驱动一级导航、快捷入口与全局搜索；模块管理页现在展示全部静态 Web 模块。使用 Playwright 完成 4 项验收，全部通过。
- 2026-08-04（第十批）：新增 `/api/auth/logout` 服务端登出接口，后端用 Redis 对当前 token 做黑名单失效，前端退出前先调用该接口再清本地状态。使用 Playwright 完成 3 项验收，全部通过。
- 2026-08-04（第十一批）：后端默认模块清单补齐仪表盘、人事、项目、考勤、考试、合规、设置等 Web 模块，模块管理从首次初始化开始即可完整驱动导航。
- 2026-08-04（第十二批）：补全合规出差与请假出差页面类型，移除该批 `any`，并修复出差列表响应取值错误；使用 Playwright 完成 4 项回归验收，全部通过。
- 2026-08-04（第十三批）：补全合规看板接口与图表类型，移除该页 `any`，并修复合规看板响应取值错误；使用 Playwright 完成 3 项验收，全部通过。
- 2026-08-04（第十四批）：将 `report/index.vue` 统计看板抽取为独立 `ReportStatsPanel` 子组件，巨型日报页完成第一步拆分；使用 Playwright 完成 2 项验收，全部通过。
- 2026-08-04（第十五批）：将日报“人员看板”抽取为独立 `ReportWorkersPanel` 子组件，通过 props/emits 保持原交互；使用 Playwright 完成 3 项验收，全部通过。
- 2026-08-04（第十六批）：将日报“回收站”抽取为自管理数据的 `ReportTrashPanel` 子组件，恢复后通过事件通知父页刷新查询；使用 Playwright 完成 3 项验收，全部通过。
- 2026-08-04（第十七批）：将 PRD 缺失模块从单条大项拆分为公告、消息、资产、操作日志、运行监控、数据备份、审批流程、个人中心 8 个独立待办，可直接进入后续排期。
- 2026-08-04（第十八批）：新增后台直接开始/结束任意员工出差能力。后端以 `attendance_leave_requests` 为主数据并同步 `biz_trip_status`，前端“出差管理”页改为员工出差状态 + 出差记录双视图，支持搜索、补录考勤、开始出差、结束出差；同时修复 `complianceApi` 双 `/api` 前缀导致的记录页 404。使用 Playwright 完成开始/结束/记录页验收，全部通过。
- 2026-08-04（第十九批）：新增 `/api/project/reviewBatch` 原子批量审核接口，前端只允许勾选待审核日报并直接调用批量接口，替换原先逐条调用；使用真实临时日报完成批量通过、批量驳回及混入已审核记录回滚验证，Playwright 验收通过，测试数据已清理。
- 2026-08-04（第二十批）：用户管理补 Excel 导入模板、Excel 全量导出和 Excel 文件导入，原有 JSON/CSV 导入保留；使用 Playwright 下载验收，模板与全量导出均为有效 xlsx 文件。
- 2026-08-04（第二十一批）：实现操作日志模块，对接 `operation_logs` 表，补齐分页列表、关键字/模块/操作/日期筛选、CSV 导出，并挂入“设置”导航；使用 Playwright 完成列表、筛选、侧边栏激活和导出验收。
- 2026-08-04（第二十二批）：生产发布完成。备份线上 `backend/src` 与 `admin` 静态目录后，发布当前本地已验收版本并重启 PM2；线上 `/api/health` 正常，新出差管理与操作日志接口返回认证错误而非 404，Playwright 在生产域名验证“员工出差状态”和“操作日志”页面均正常加载。
- 2026-08-04（第二十三批）：个人中心补齐“编辑资料”和“修改密码”，后端新增 `POST /api/user/change-password`；使用临时账号完成资料更新、密码修改和密码校验，临时账号已删除。改动已再次发布生产，线上个人中心可见新按钮。
- 2026-08-04（第二十四批）：公告管理实现列表/搜索/状态与优先级筛选/新建/编辑/发布/下线/删除，后端新增 `/api/announcement/*` 并挂入 Web 导航；本地 Playwright 完成公告全生命周期验收，临时公告已清理，生产已验证公告页与接口。
- 2026-08-04（第二十五批）：Web“出差管理”从合规模块移动到考勤模块，路由调整为 `/attendance/biz-trip`；前端已部署生产，线上验证考勤侧边栏激活“出差管理”，合规模块不再显示该入口。
- 2026-08-04（第二十六批）：修复出差管理 500 漏洞。`getBizTripList` 误用不存在的 `u.avatar` 字段并错误解构总数查询，已改为 `u.avatar_url` 并修正 `total` 取值；生产接口与“员工出差状态/出差记录”两个 Tab 验证通过。
- 2026-08-05（第二十七批）：小程序首页“未填写”新增明细页。后端新增 `/api/stats/missing-details`，管理员返回全员缺填明细、员工只返回本人；未填写日期只统计今天之前且处于出差期间的日期，当天不计入。小程序构建通过，生产接口已验证。
- 2026-08-05（第二十八批）：修复公出统计日历人数口径。`getDailyCounts` 改为统计非草稿实际填写人数，兼容补公出待审核和日志文本代填；请假/公司日报人员不再计入总需填写人数。8/4 生产验证为 `15/16`，未提交仅 1 人。
- 2026-08-04（第二十七批）：修复小程序公出统计“明日状态”人员口径。`getTomorrowStatus` 原按全部在职人员取数，已改为复用对应“今日状态”的人员集合，确保明日状态人数与今日状态一致；生产接口验证今日/明日均为 19 人且人员 ID 一致。
- 2026-08-04（第二十八批）：日报查询移除普通日报“通过/驳回”和“批量通过/批量驳回”审核按钮，审核统一收敛到补公出审核入口；生产浏览器验证日报查询已无审核按钮。
- 2026-08-04（第二十九批）：日报查询操作列移除补公出“审核”按钮，审核全部收敛到独立补公出审核页面；生产浏览器验证日报查询操作列已无任何审核按钮。

---

## 2. 全局框架与导航

| ID | 优先级 | 模块 | 问题 | 位置 | 建议方案 | 验收标准 | 依赖/状态 |
|----|:----:|------|------|------|----------|----------|-----------|
| G-01 | P0 | 路由权限 | 路由守卫只校验 token，未按角色或权限拦截；员工可直接访问 `/user`、`/role` 等管理页面 | `webapp/src/router/index.ts` | 为路由补充 `meta.roles` 或 `meta.permissions`，在 `beforeEach` 中校验；无权限时跳转 403 页 | 普通员工访问管理路由时被拦截并提示无权限，不出现空白或越权页面 | 已完成 2026-08-04 |
| G-02 | P0 | 顶栏 | “个人中心”菜单跳转 `/profile`，该路由不存在，点击后进入 404 | `webapp/src/components/TopBar/index.vue`；`webapp/src/router/index.ts` | 新增个人中心页，或本轮先移除该菜单项 | 点击“个人中心”不再 404；保留入口时必须能正常打开页面 | 已完成 2026-08-04 |
| G-03 | P0 | 顶栏 | 搜索框、通知铃铛没有事件处理，`Ctrl+K` 仅为视觉提示；点击无反馈 | `webapp/src/components/TopBar/index.vue`；`webapp/src/layouts/DefaultLayout.vue` | 实现全局菜单搜索和通知入口；若通知 API 未就绪，先移除铃铛或改为禁用态 | 所有可见顶栏控件均有明确反馈，不留下死按钮 | 已完成 2026-08-04 |
| G-04 | P1 | 布局 | 无面包屑、动态 `document.title`、侧边栏折叠和小屏适配；内容区固定左边距 236px | `webapp/src/layouts/DefaultLayout.vue`；`webapp/src/router/index.ts`；`webapp/src/components/PrimaryNav/index.vue`；`webapp/src/components/ModuleSidebar/index.vue` | 增加布局 store、折叠按钮、面包屑、路由标题；小屏时侧边栏改为抽屉或遮罩 | 桌面端可折叠导航，小屏可正常操作，页面标题与路由一致 | 已完成 2026-08-04 |
| G-05 | P1 | 仪表盘 | 快捷入口未按角色过滤，员工也能看到用户/审批等管理入口 | `webapp/src/views/dashboard/index.vue` | 复用 `getVisibleModules` 或新增按角色过滤方法 | 不同角色看到的快捷入口与导航权限一致 | 已完成 2026-08-04 |

---

## 3. 登录与认证

| ID | 优先级 | 模块 | 问题 | 位置 | 建议方案 | 验收标准 | 依赖/状态 |
|----|:----:|------|------|------|----------|----------|-----------|
| AUTH-01 | P1 | 登录 | 企业微信 OAuth 回调只处理成功路径，`refreshProfile` 失败时可能带着无效 token 进入后台 | `webapp/src/views/login/index.vue` | 捕获资料刷新失败，清空 token 和 userInfo，提示重新登录；成功后再跳转首页 | OAuth token 无效或资料接口失败时停留在登录页并给出错误提示 | 已完成 2026-08-04 |
| AUTH-02 | P1 | 登录 | `handleLogin` 直接 `await formRef.value.validate()`，校验失败会产生未捕获 Promise 拒绝 | `webapp/src/views/login/index.vue` | 使用 `try/catch` 包裹校验，校验失败时聚焦首个错误字段 | 表单校验失败不产生控制台异常，用户能看到明确错误 | 已完成 2026-08-04 |
| AUTH-03 | P1 | 登录 | 滑动验证失败后通过 `setTimeout` 循环刷新，页面卸载或重复提交时没有清理和防重 | `webapp/src/views/login/index.vue` | 记录 timer，`onUnmounted` 清理；登录按钮 loading 期间禁止重复提交 | 卸载页面后不继续触发验证码刷新，登录中不能重复提交 | 已完成 2026-08-04 |
| AUTH-04 | P2 | 登录 | 退出登录没有二次确认，也未调用服务端失效接口 | `webapp/src/components/TopBar/index.vue`；`webapp/src/views/profile/index.vue`；`backend/src/auth/routes/auth.routes.js` | 增加确认弹窗，并复用现有登出接口后再跳转登录页 | 退出前有确认，退出后 token 被服务端失效 | 已完成 2026-08-04 |

---

## 4. 用户/组织/角色

| ID | 优先级 | 模块 | 问题 | 位置 | 建议方案 | 验收标准 | 依赖/状态 |
|----|:----:|------|------|------|----------|----------|-----------|
| USER-01 | P0 | 角色 | “角色成员”Tab 是“即将上线”占位，但 PRD 要求角色用户分配 | `webapp/src/views/role/index.vue` | 补齐角色成员列表、添加成员、移除成员；保存后刷新角色权限 | 管理员可在角色页完成成员分配并立即生效 | 已完成 2026-08-04 |
| USER-02 | P0 | 角色 | 创建角色时 `groupId` 未随创建提交，新角色不会归入所选分组 | `webapp/src/views/role/index.vue`；`webapp/src/api/role.ts`；`backend/src/core/controllers/admin.controller.js`；`backend/src/core/services/admin.service.js` | 修复创建角色的参数和更新链路，确保分组选择被保存 | 新建角色后出现在所选角色分组下 | 已完成 2026-08-04 |
| USER-03 | P1 | 用户 | 用户管理缺少部门筛选、批量导入导出、模板下载、重置密码、详情查看、批量操作、每页条数切换 | `webapp/src/views/user/index.vue`；`webapp/src/api/user.ts` | 按 PRD 补齐工具栏与弹窗，API 已有能力直接接入 | 用户页满足 PRD 的搜索、导入导出、重置密码和批量操作验收 | 已完成 2026-08-04：详情/重置密码/批量操作/JSON-CSV导入/CSV导出/模板已补；Excel模板、Excel全量导出与Excel导入已补并浏览器验收通过 |
| USER-04 | P1 | 用户 | 新建/邀请用户表单未使用 Element Plus 表单校验，`openid` 标必填但未校验，部门传 name/id 不统一 | `webapp/src/views/user/index.vue` | 统一使用 FormRules；部门选择统一提交 id；明确必填字段 | 表单校验与提交参数一致，不再出现“必填但可提交空值” | 已完成 2026-08-04：新建/邀请表单校验已补 |
| USER-05 | P1 | 花名册 | “快速选择”弹窗关闭后不重置，重复新增会残留上一次选择 | `webapp/src/views/user/workers.vue` | 弹窗 `@closed` 时重置 `selectedNonRosterId` 和表单 | 连续打开新增弹窗时始终是干净表单 | 已完成 2026-08-04 |
| ORG-01 | P2 | 组织 | 右键菜单不做边界处理，靠近视口边缘时可能溢出 | `webapp/src/views/org/index.vue` | 计算菜单位置并限制在视口内 | 在任意滚动位置打开右键菜单都不会溢出屏幕 | 已完成 2026-08-04 |

---

## 5. 日报/统计/公出

| ID | 优先级 | 模块 | 问题 | 位置 | 建议方案 | 验收标准 | 依赖/状态 |
|----|:----:|------|------|------|----------|----------|-----------|
| REPORT-01 | P1 | 日报 | 日报查询缺少批量审核、表格多选、日期范围校验、pageSize 切换；导出 fetch 硬编码 `/api` 而非 `VITE_API_BASE_URL` | `webapp/src/views/report/index.vue`；`webapp/src/api/report.ts`；`backend/src/features/services/review.service.js`；`backend/src/features/routes/review.routes.js` | 校验 startDate 不晚于 endDate；统一走 request 或 baseURL；审核统一放到补公出审核页面 | 日期范围错误有提示，导出在非根路径部署下仍可用；日报查询操作列无任何审核按钮 | 已完成 2026-08-04：日期校验/pageSize/导出 baseURL 已补；日报查询操作列审核按钮已全部移除 |
| REPORT-02 | P1 | 公出统计 | 多处日期用 `toISOString().slice(0,10)`，东八区夜间会退一天 | `webapp/src/views/report/daily-status.vue`；`webapp/src/views/attendance/ScheduleRules.vue` 等 | 新增北京日期工具并替换 Web 端使用点 | 日期展示和查询结果与北京时间一致 | 已完成 2026-08-04 |
| REPORT-03 | P1 | 日报编辑 | 编辑弹窗对已提交/已通过记录没有只读或二次确认，字段缺少必填和范围校验 | `webapp/src/views/report/index.vue` | 按状态限制编辑，提交前确认；补日期、数量、工作内容校验 | 已通过记录不会被无提示修改，非法表单不能提交 | 已完成 2026-08-04 |
| REPORT-04 | P1 | 月度占比 | 人员下拉固定取前 200 人，无搜索，大组织会漏人 | `webapp/src/views/report/monthly-summary.vue` | 改为远程搜索/分页选择 | 可通过姓名或工号搜索到任意在册人员 | 已完成 2026-08-04 |
| REPORT-05 | P2 | 日报 | `report/index.vue` 为巨型组件，统计、查询、人员、回收站、审核、编辑耦合在一起 | `webapp/src/views/report/index.vue`；`webapp/src/components/ReportStatsPanel.vue`；`webapp/src/components/ReportWorkersPanel.vue`；`webapp/src/components/ReportTrashPanel.vue` | 按 Tab 和弹窗拆分为独立组件 | 各子模块可独立维护，页面交互行为不回退 | 部分完成：统计看板、人员看板、回收站已抽取；查询/审核/编辑待续 |

---

## 6. 审批/合规/考勤/考试/项目/设置

| ID | 优先级 | 模块 | 问题 | 位置 | 建议方案 | 验收标准 | 依赖/状态 |
|----|:----:|------|------|------|----------|----------|-----------|
| BIZ-01 | P0 | 合规-出差 | `loadUsers()` 是空实现，员工下拉永远为空，无法设置出差 | `webapp/src/views/compliance/BizTripManage.vue` | 接入用户/花名册 API，补齐员工选择和字段映射 | 设置出差时可选择员工并提交成功 | 已完成 2026-08-04 |
| BIZ-02 | P0 | 合规-出差 | 已出差但未发起出差申请的人员无法在后台直接开始/结束出差 | `webapp/src/views/compliance/BizTripManage.vue`；`webapp/src/api/attendance.ts`；`webapp/src/api/compliance.ts`；`backend/src/features/attendance/services/trip.service.js`；`backend/src/features/attendance/routes/attendance.routes.js` | 新增管理员接口直接为任意员工开始/结束出差，以考勤出差记录为主数据并同步合规记录；前端提供全员状态列表与操作弹窗 | 管理员可为未申请员工选择开始日期开始出差，也可直接结束；状态在考勤与合规两侧同步 | 已完成 2026-08-04：接口与前端页面已实现，浏览器验收通过；`complianceApi` 双 `/api` 前缀已修复 |
| APPROVAL-01 | P1 | 审批 | 审批列表无分页、无搜索、无详情/处理操作；配置 Tab 无法新增审批类型 | `webapp/src/views/approval/index.vue`；`webapp/src/api/approval.ts`；`backend/src/core/routes/admin.routes.js` | 补审批实例分页、搜索、详情与状态操作；配置 Tab 补新增类型 | 审批列表可完整浏览和处理，配置页可新增类型 | 已完成 2026-08-04 |
| COMPLIANCE-01 | P1 | 合规 | 合规看板 ECharts 未在卸载时 dispose，重复初始化可能泄漏；无 loading 和空状态 | `webapp/src/views/compliance/Dashboard.vue` | 保存 chart 实例，`onUnmounted` 中 dispose 并移除 resize 监听；补 loading/empty | 页面切换和重复刷新后图表正常且无内存泄漏 | 已完成 2026-08-04 |
| ATTENDANCE-01 | P1 | 考勤 | 请假出差表有重复“申请人”列，分页只绑 `@change`，size-change 不完整 | `webapp/src/views/attendance/LeaveManage.vue` | 去重列；补齐 current-change 与 size-change 处理 | 表格列不重复，分页和每页条数均正确刷新 | 已完成 2026-08-04 |
| EXAM-01 | P1 | 考试 | 试卷”查看”按钮是空 TODO，点击无反应 | `webapp/src/views/exam/papers.vue` | 补只读试卷预览，或未实现前移除按钮 | 已发布试卷可查看完整内容，或不再出现无响应按钮 | 已废弃：旧考试模块整体删除，合并重建为答题模块（v2.0）；papers.vue 已删除 |
| EXAM-02 | P1 | 考试 | 题目编辑只提交假选项，无法维护选项文本和正确项 | `webapp/src/views/exam/questions.vue` | 增加选项编辑器，按题型展示单选/多选/判断选项并接入 options 字段 | 新增和编辑题目可维护真实选项，保存后列表/预览正确 | 已废弃：旧考试模块整体删除，合并重建为答题模块（v2.0）；新题库管理已实现选项编辑器，待端到端验收 |
| PROJECT-01 | P1 | 项目 | 项目管理页实际只是日报按项目聚合，没有项目 CRUD/详情，名称有误导 | `webapp/src/views/project/index.vue`；`webapp/src/api/project.ts` | 补齐项目列表/新建/详情，或先改名为“项目日报汇总” | 页面名称与功能一致；若保留现状则不再声称项目管理 | 部分完成：真实项目列表/详情已接入；新建与任务看板待后端 API |
| SETTINGS-01 | P1 | 设置 | 系统设置数字/时间字段无类型校验和保存确认 | `webapp/src/views/settings/index.vue` | 按配置 key 定义输入类型，校验数字、时间和必填项，保存前确认 | 非法配置无法保存，保存前有明确确认 | 已完成 2026-08-04 |
| MODULES-01 | P2 | 模块管理 | 模块管理保存后不会驱动 Web 导航动态变化 | `webapp/src/views/modules/index.vue`；`webapp/src/config/modules.ts`；`webapp/src/stores/module.ts`；`backend/src/core/services/module.service.js` | 将模块配置接入静态配置或动态路由渲染 | Web 导航与模块开关/角色配置一致 | 已完成 2026-08-04 |

---

## 7. PRD 遗漏模块

| ID | 优先级 | 模块 | 问题 | 位置 | 建议方案 | 验收标准 | 依赖/状态 |
|----|:----:|------|------|------|----------|----------|-----------|
| PRD-01 | P2 | 公告管理 | 公告发布、编辑、下线、置顶、阅读统计、范围控制未实现 | `webapp/src/router/index.ts`；`webapp/src/views/announcement/index.vue`；`backend/src/core/services/announcement.service.js`；`backend/src/core/routes/announcement.routes.js`；`.AI/Wiki/Web 管理后台/Web-PRD.md` | 按 PRD 拆为独立模块，补齐页面与后端接口 | 公告全生命周期可用 | 部分完成 2026-08-04：新建/编辑/发布/下线/删除/列表筛选已实现并发布；阅读统计与范围控制待后续 |
| PRD-02 | P2 | 消息管理 | 消息模板、手动推送、推送记录未实现 | `webapp/src/router/index.ts`；`.AI/Wiki/Web 管理后台/Web-PRD.md` | 按 PRD 拆为独立模块，补齐模板与推送记录 | 模板可配置、推送可记录 | 依赖后端模块接口 |
| PRD-03 | P2 | 资产管理 | 资产台账 CRUD、领用审批、盘点、报表未实现 | `webapp/src/router/index.ts`；`.AI/Wiki/Web 管理后台/Web-PRD.md` | 按 PRD 拆为独立模块，补齐资产生命周期 | 资产台账与盘点可用 | 依赖后端模块接口 |
| PRD-04 | P2 | 操作日志 | 操作审计列表、搜索、导出未实现 | `webapp/src/router/index.ts`；`webapp/src/views/logs/index.vue`；`backend/src/core/controllers/operation-log.controller.js`；`backend/src/core/services/operation-log.service.js`；`backend/src/core/routes/admin.routes.js` | 按 PRD 拆为独立模块，对接 operation_logs | 操作日志可按条件查询导出 | 已完成 2026-08-04：列表/关键字与模块/操作/日期筛选/CSV导出已实现并浏览器验收通过 |
| PRD-05 | P2 | 运行监控 | 服务健康状态、API 统计、错误告警未实现 | `webapp/src/router/index.ts`；`.AI/Wiki/Web 管理后台/Web-PRD.md` | 按 PRD 拆为独立模块，对接健康与统计接口 | 运行状态可视化 | 依赖后端接口 |
| PRD-06 | P2 | 数据备份 | 手动备份、自动备份策略、恢复入口未实现 | `webapp/src/router/index.ts`；`.AI/Wiki/Web 管理后台/Web-PRD.md` | 按 PRD 拆为独立模块 | 可查看备份记录并执行策略 | 依赖运维能力 |
| PRD-07 | P2 | 审批流程 | 审批模板、流程设计器、超时监控未实现 | `webapp/src/router/index.ts`；`.AI/Wiki/Web 管理后台/Web-PRD.md` | 按 PRD 拆为独立模块，补齐流程配置 | 流程可配置并监控超时 | 依赖后端接口 |
| PRD-08 | P2 | 个人中心 | 个人资料编辑、修改密码、通知偏好未完整实现 | `webapp/src/views/profile/index.vue`；`webapp/src/api/auth.ts`；`backend/src/auth/services/auth.service.js`；`backend/src/auth/routes/auth.routes.js`；`.AI/Wiki/Web 管理后台/Web-PRD.md` | 基于现有个人中心页补齐资料/密码/偏好 | 个人中心完整可用 | 部分完成 2026-08-04：资料编辑与修改密码已实现并发布；通知偏好待后续 |

---

## 8. 质量与文档

| ID | 优先级 | 模块 | 问题 | 位置 | 建议方案 | 验收标准 | 依赖/状态 |
|----|:----:|------|------|------|----------|----------|-----------|
| QUALITY-01 | P1 | 代码质量 | 存在 `console.error`、TODO 死按钮和注释代码，违反提交前清理约束 | `webapp/src/views/compliance/BizTripManage.vue`；`webapp/src/views/exam/papers.vue` | 清理日志、空实现和死代码；无法实现的入口先移除 | `rg` 检查无新增 console/debugger/TODO 死代码 | 已完成 2026-08-04 |
| QUALITY-02 | P2 | 类型 | 新模块大量使用 `any`，类型安全不足 | `webapp/src/views/compliance/`；`webapp/src/views/exam/`；`webapp/src/views/attendance/` | 按 API 类型补齐接口返回类型，移除 `any` | `rg` 检查主要模块无新增 `any` | 部分完成：BizTrip/LeaveManage/Compliance Dashboard 类型已补；其余模块待续 |
| QUALITY-03 | P2 | 性能 | Element Plus 和 ECharts 全量导入，Vite 未做 chunk 分割 | `webapp/src/main.ts`；`webapp/vite.config.ts` | 按需引入组件和图表模块，配置分包/代码分割 | 构建体积下降，首屏加载不回归 | 部分完成：Vite vendor 分包已补；Element Plus/ECharts 按需引入待后续 |
| QUALITY-04 | P2 | 文档 | Wiki/README 仍引用不存在的 `AppHeader`、`AppSidebar`、`stores/app.ts`，与真实组件不一致 | `.AI/Wiki/Web 管理后台/`；`webapp/README.md` | 将文档同步为 `TopBar`、`PrimaryNav`、`ModuleSidebar` 和真实目录结构 | 文档引用的组件和文件均存在于仓库 | 已完成 2026-08-04 |

---

## 9. 待确认问题

1. 出差管理员工来源已确认：使用全员用户列表，后端返回 `userId/userName/workerCode/departmentName`，前端不再依赖花名册。
2. 个人中心是补齐完整资料/密码/通知偏好页面，还是本轮先隐藏入口，需产品确认。
3. 通知铃铛若后端消息 API 未就绪，是否允许先移除伪交互，需产品确认。
4. 项目管理是补齐真实项目 CRUD，还是先改名“项目日报汇总”，需产品确认。
5. PRD 遗漏模块是否进入当前迭代排期，还是仅登记为 P2 后续计划。

---

## 10. 验收方式

- 本文件创建成功且非空，包含清单摘要、优先级定义、模块分组表格、待确认问题和验收方式。
- 每个待办项均包含 `ID / 优先级 / 模块 / 问题 / 位置 / 建议方案 / 验收标准 / 依赖状态`。
- 本轮只改动 `大纲/需求修改/待办.md`，不修改 `webapp/src` 业务代码。
- 基线 `webapp` 的 `npm run type-check` 已通过；后续执行任一待办时需再次通过 type-check、lint 和构建。
