'use strict';

/**
 * 服务层入口
 * 后续模块在此导出所有服务
 *
 * 示例：
 * const userService = require('./user.service');
 * const approvalService = require('./approval.service');
 *
 * module.exports = {
 *   userService,
 *   approvalService,
 * };
 */

module.exports = {};
