# 2026-05-25 工作日志

## 修复 miniapp config 模块缺失问题

**问题**: `pages/employee/project-edit` 报错 `module 'config/index.js' is not defined`
**原因**: `miniapp/config/` 目录不存在，`request.js`/`project.js`/`report.js`/`review.js` 均引用 `../config/index`
**修复**: 创建 `Y:\AI\WX-APP-OA\miniapp\config\index.js`，包含 BASE_URL、TIMEOUT、storageKeys、apiPaths 完整配置

## Ubuntu 服务器迁移后服务恢复

**背景**: 服务器从 CentOS 7 迁移到 Ubuntu 24.04，数据不恢复（重新开始）

**完成操作**:
1. 检查确认 Node.js/MySQL/Nginx/PM2 均已安装运行
2. 发现旧版 app.js（675行）缺少项目路由和排班路由
3. 上传本地最新版 `Y:\AI\WX-app\daily-report-api\server.js`（含排班路由）
4. 将 `server_new.js` 中的完整项目路由（`/api/project/*`）合并入 server.js
5. 修复 `db.js` 的 LIMIT 参数 bug：`pool.execute` → `pool.query`（前者不支持 LIMIT 绑定参数）
6. 创建排班相关数据表：`schedule_daily`、`schedule_workers`
7. 重新配置 `ecosystem.config.js`：`fork` 单实例模式，脚本指向 `server.js`
8. PM2 服务已启动并设置开机自启（`systemctl enable pm2-root`）

**当前服务状态**:
- API 服务: online，端口 3000
- Nginx: active，端口 80
- 数据库: daily_report（5 张表，空数据）
- 开机自启: 已配置

**已验证接口**:
- `/api/login` ✅
- `/api/project/list` ✅
- `/api/report/list` ✅
- `/api/schedule/month` ✅
- `/api/admin/list` ✅
- `/health` ✅

**关键修复记录**:
- db.js query 方法用 pool.execute 时 LIMIT ? 参数会报 ER_WRONG_ARGUMENTS，必须用 pool.query
- PM2 缓存旧配置时用 `pm2 delete + pm2 start` 而不是 `pm2 restart` 才能生效新脚本路径
- BugPack MCP Server 已配置到 `C:\Users\WarBlood\.workbuddy\.mcp.json`（stdio 模式，npx bugpack-mcp --mcp）
