# 2026-05-28 工作日志

## 修复 miniapp 编译失败问题

**问题**：HBuilder 编译微信小程序时提示 "node_modules缺少编译器模块"

**排查过程**：
- miniapp 项目为 uni-app (vue3) + mp-weixin 架构
- node_modules/@dcloudio 下的包已存在，但依赖树不完整（缺少 package-lock.json）
- 先后执行 `npm install`，第一次修复 4 包，第二次修复 52 包+变更 30 包
- esbuild 原生二进制已确认到位

**结果**：
- 命令行 `npx uni build -p mp-weixin` 编译成功
- Compiler version 5.11（vue3），DONE Build complete
