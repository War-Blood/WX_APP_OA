# 工作日报 — 2026-08-14

---

## 今日工作内容

- 员工当日状态页（Web）接入公出统计同款动态筛选，今日/明日各用独立视图，端到端部署上线。

## 明日工作计划

- [ ] 观察生产环境「员工当日状态」筛选配置是否正常生效
- [ ] （可选）为 daily_today / daily_tomorrow 配置默认部门/仅现场条件

## 遇到的问题

- 无

## 需协调事项

- 协调 1：前端「筛选」弹窗仅 admin+ 可见；如需开放给部门领导配置，需产品确认

---

## 开发补充（提报公司日报时删除本段）

### 完成明细
- 员工当日状态页接入动态筛选：`stats-view.service.js`（VALID_KEYS 新增 `daily_today`/`daily_tomorrow`）、`stats.service.js`（getDailyStatus/getTomorrowStatus 支持 statKey，默认 daily 兼容小程序）、`report.controller.js`（透传 statKey）、`report.ts`（参数类型）、`daily-status.vue`（复用 FilterDialog，今日/明日独立视图，`7a46b9b`）
- 公出统计双问题修复：
  1. 员工端校验日历口径：`stats.service.js` `buildUserFilter` 新增视图级 RLS 默认覆盖 `VIEW_SCOPE_DEFAULTS`，`calendar` 视图对普通员工默认 `self` 范围，日历只统计本人「已提交/需提交」（原部门级 `21/6` → `1/1`）
  2. 员工端隐藏「人员分布-区域分布」：`stats.service.js` `getAreaDistribution`/`getProvinceWorkers` 对 `employee` 角色直接返回空集；`miniapp/src/pages/profile/stats.vue` + `stores/user.js` 按 `isEmployee` 隐藏区域分布卡片并跳过加载
- Web 构建部署：`npm run build` → 备份 `admin.bak.20260814_091805` → scp 至 `/var/www/wx-app-oa/admin/`
- 后端三文件部署：备份 `*.bak.20260814_*` → 替换 → PM2 重启（pid 3332343 online）
- 小程序「公出统计-全员当日」卡片左侧色条按工作状态着色：`daily-status-panel/index.vue`（待工=淡黄/工作=正常绿/在途=蓝/请假=紫，代填同理，无工作类型回退状态色，`5cd0671`，版本 1.0.2.47）

### 部署状态
| 层 | 状态 | 备注 |
|------|:--:|------|
| 后端 | ✅ | PM2 重启，健康检查 DB/Redis ok |
| 小程序 | 🔜 | 已构建 dist/build/mp-weixin，待微信开发者工具上传 |
| Webapp | ✅ | 已构建部署，`/` 与 index 均 200 |

### 验证结果
- `https://warblood.online/` 200，新 chunk（daily-status/FilterDialog）可访问 ✅
- 生产 API：daily-status(statKey=daily_today) code=0（22 人）、tomorrow-status(statKey=daily_tomorrow) code=0 ✅
- stats/views 保存 daily_today 成功（旧后端会报「无效的统计页标识」）✅
- 今日/明日独立视图：daily_today 视图行已落库（id=43，默认空条件）✅

### 待处理
- [ ] 小程序公出统计「全员当日」仍走原 daily 视图，未受影响（如需同步今日筛选可后续评估）

---

*自动生成*
### 答题模块四合一改造（批量导入完善/题目图片/扁平化主分类/Web交互修复）

- 需求1 批量导入完善：webapp/src/utils/excel.ts 模板新增「分类」「题干图片」列（动态识别表头，兼容旧模板）；解析支持分类名→ID 与图片 URL 透传；新增 downloadImportErrors 错误明细 CSV 下载。question.service.js batchImport 服务端答案/分值/判分模式校验、分类一次校验、chunk 批量写入、baseRow 使错误行号对齐 Excel；question.controller.js 透传 baseRow + 审计日志。questions.vue 导入弹窗增加目标分类下拉、on-exceed 提示、成功后自动关闭、错误明细下载。
- 需求2 题目图片：后端新增 POST /api/exam/upload/image（multer + sharp 校验，backend/uploads/question/，app.js 挂载 /uploads 静态）；exam_questions 新增 title_image/analysis_image 列（init-db v3.1 幂等迁移 + sql/v4_exam_image_columns.sql）；快照链路（startLearn/startTimed/startPaperExam/gradeSnapshot）携带图片字段，shuffleOptions 保留选项 image；Web 题目表单支持题干/解析/选项图片上传与预览、分类封面上传；小程序 question-card 渲染题干/选项/解析图片。
- 需求3 移除子分类：category.service.js list 返回扁平主分类（去 children/子树聚合）、create/update/remove 去子分类逻辑；question.service.js 移除「低压电工」硬编码回落（显式分类校验/唯一分类回落/多分类必填）；exam.service.js getCategorySubtreeIds 扁平化；webapp 六页与小程序分类页全面去 flatten；PRD 01/02/05/08 与需求文档同步。
- 需求4 Web 交互缺失修复：P0 —— record detail 管理员可查看任意记录（原必 404）、试卷手动选题分页+跨页选中保留+编辑回显、题目答案合法性前后端校验；P1 —— 成绩详情答案显示选项文本与每题得分、试卷查看预览/归档/克隆、scope=user 姓名回显、random 规则「全部」选项、部门树回显修复、合格分≤总分校验、按试卷筛选、统计通过占比与分数段、答题设置 check_user 开关启用、题库状态列。
- 测试：exam-question.service.test.js 重写 17 项全过；webapp vue-tsc --noEmit 零错误 + npm run build 通过；小程序 build:mp-weixin 通过（v1.0.2.49）。
