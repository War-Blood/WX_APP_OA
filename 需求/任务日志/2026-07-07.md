# 工作日志 — 2026-07-07

---

## 完成事项

| # | 事项 | 涉及文件 | 提交 |
|:--:|------|------|------|
| 1 | 公司级排班表重构 | `company_schedules` 表 + schedule.service/controller/routes + report/summary.service + Schedule.vue + attendance.ts + my-schedule.vue | 未提交(已部署) |
| 2 | Web 端排班规则页(ScheduleRules) | ScheduleRules.vue + attendance.ts | 未提交(已部署) |
| 3 | Web 端清理 + 菜单重命名 | 删除 Schedule/Summary.vue, router meta `考勤管理`→`出勤日历`, Sidebar 子菜单 | 未提交(已部署) |
| 4 | 后端 MySQL Date 格式化 Bug 修复 | schedule/report/summary.service.js 3 处 `String(date).slice`→手动格式化 + 旧表名 attendance_schedules→company_schedules | 未提交(已部署) |
| 5 | 日报加班表对接出勤日历 | report/index.vue 调 `/attendance/schedule/preview` + report.service exportStatusBoardCSV | 未提交(已部署) |
| 6 | 审核弹窗关闭 & 数据刷新 + 出差时间列修复 | report/index.vue, report/audit.vue, report.service.js CSV 日期 | 未提交(已部署) |
| 7 | 出差状态管理 + 请假自动联动公出统计 | users.biz_trip_status + stats/admin/leave.service + user.ts + user/index.vue | 未提交(已部署) |
| 8 | 请假取消时区 Bug + 日志覆盖 | leave.service.cancel() + report.service.submit() + formatRequest/calcMissingDates 8处 toISOString→fmtDate | 未提交(已部署) |
| 9 | 小程序 URLSearchParams 兼容性修复 | leave-list/index.vue, leave-detail/index.vue | 未提交(已构建) |
| 10 | 首页模块排序 + 考勤打卡 icon | system_config.module_visibility + 新建 quick-clock.svg | 未提交(已构建) |

## 部署状态

| 层 | 状态 | 备注 |
|------|:--:|------|
| 后端 | ✅ | PM2 重启, 健康检查 ok (db 3ms / redis 0ms), uptime ~40min |
| 小程序 | ✅ | 已构建 dist/build/mp-weixin (14:21), 待微信开发者工具重新导入并上传 |
| Webapp | ✅ | 多次构建部署, https://warblood.online/admin/ 可访问 |

## 验证结果

- 公司级排班 preview ✅ (2026-07: workDays=23, restDays=8)
- 大小周交替规则 ✅ (7-4 work / 7-11 rest)
- 出差状态 17 人设为 field ✅
- 请假取消按钮在小程序可见 ✅ (formatRequest toISOString 修复后)
- 小程序 URLSearchParams 报错消除 ✅ (dist 无残留)
- 首页常用功能显示 4 模块 ✅ (公出日志/日报历史/公出统计/考勤打卡)
- 考勤打卡 icon 正常显示 ✅

## 待处理

- [ ] **7/7 代码尚未 git commit**（含后端 10+ 文件、miniapp 4 文件、webapp 2 文件、sql 迁移脚本）
- [ ] 小程序 dist 需在微信开发者工具重新导入并上传发布
- [ ] 微信订阅消息提醒（可行性已确认，未实现）：需注册订阅消息模板 + 小程序授权弹窗 + wechat template.service.js 改造为 `subscribe/send` + 定时任务改 23:00
- [ ] ScheduleRules 页面「大小周双周配置 + 节假日管理」逻辑需回归验证

---

*自动生成*
