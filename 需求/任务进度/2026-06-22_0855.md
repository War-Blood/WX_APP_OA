# 任务进度 — 2026-06-22_0855（1.md 完善完成）

> 分支: `feature/v2.0-report` | 状态: 1.md 三个需求全部完成 ✅

---

## 1.md 三个需求最终状态

| # | 需求 | 状态 | 关键改动 |
|:--:|------|:--:|------|
| 1 | 登录用户验证 | ✅ | 禁止未登记用户自动注册 + deleted_at 全面过滤 |
| 2 | 昵称修改 | ✅ | 字段名统一 + Store 同步 + 首次登录昵称映射 |
| 3 | 隐私政策弹窗 | ✅ | @tap.stop 弹出完整协议文本 |

### 需求1 改动细则

- `auth.service.js` login(): 移除自动 INSERT pending 逻辑，改为 `throw BusinessError('账号未注册')`
- `auth.service.js` qywxLogin(): 同上
- `auth.service.js` deleted_at 过滤覆盖: login/getProfile/qywxLogin/adminLogin/linkQywxAccount
- `middleware/auth.js` authenticate: deleted_at 过滤

### 需求2 改动细则

- `login/index.vue`: askNickname() `nickname`→`nickName` + userStore.setUserInfo()
- `login/index.vue`: handleLogin() 存储前 `nickname`→`nickName` 映射
- `profile/index.vue`: editNickname() 增加 userStore.setUserInfo()

### 需求3 改动细则

- `login/index.vue`: 协议链接 @tap.stop + USER_AGREEMENT/PRIVACY_POLICY 文本 + showModal 弹窗

---

## 提交记录

```
c03eaf9 fix(auth): 补漏 — 禁止未登记用户自动注册 + 登录时昵称字段映射
112de6f fix(report): Phase 2 补公出审批
795a4b4 fix(shared): Phase 1 紧急修复
```
