# 执行清单 — 2026-06-22

> 服务器: root@111.229.107.123  
> SSH 密钥: C:/Users/WarBlood/.ssh/wx_app_key.pem  
> 部署路径: /var/www/wx-app-oa/

---

## 一、重启后端服务（使新 API 生效）

```bash
ssh -i "C:/Users/WarBlood/.ssh/wx_app_key.pem" root@111.229.107.123 "pm2 restart wx-app-oa-api"
```

验证：
```bash
ssh -i "C:/Users/WarBlood/.ssh/wx_app_key.pem" root@111.229.107.123 "pm2 list"
```
确认 `wx-app-oa-api` 状态为 `online`，uptime 刷新。

---

## 二、运行数据迁移脚本（绑定 2073 条历史数据到员工）

### 先预览（安全模式，不写入）：
```bash
ssh -i "C:/Users/WarBlood/.ssh/wx_app_key.pem" root@111.229.107.123 "cd /var/www/wx-app-oa/backend && node scripts/migrate-workers-to-userid.js --dry-run"
```

### 确认无误后执行：
```bash
ssh -i "C:/Users/WarBlood/.ssh/wx_app_key.pem" root@111.229.107.123 "cd /var/www/wx-app-oa/backend && node scripts/migrate-workers-to-userid.js"
```

**预期结果**: `daily_report_workers` 表从 0 条记录增加至 2000+ 条。

---

## 三、部署确认清单

以下文件已通过 SCP 上传，仅需确认 PM2 重启即可：

| 文件 | 改动内容 |
|------|---------|
| `backend/src/core/services/stats.service.js` | getDailyStatus 过滤修复 + 新增 daily-counts/project-progress |
| `backend/src/core/services/report.service.js` | 新增 exportAttendanceCSV |
| `backend/src/core/controllers/report.controller.js` | 新增 exportAttendance 控制器 |
| `backend/src/core/routes/report.routes.js` | 新增 /export-attendance 路由 |
| `backend/src/features/controllers/stats.controller.js` | 新增 dailyCounts / projectProgress |
| `backend/src/features/routes/stats.routes.js` | 新增 /daily-counts /project-progress 路由 |
| `backend/scripts/migrate-workers-to-userid.js` | 修复空格分隔 |
| `webapp/dist/` | Web 前端（日历热力图+项目进展+导出考勤） |
| `miniapp/dist/build/mp-weixin/` | 小程序前端（需微信开发者工具导入） |

---

## 四、新增 API 验证

重启后依次测试：

```bash
# 1. 每日状态（应包含待审核的日报，不再只返回 approved）
curl -X POST https://warblood.online/api/report/daily-status \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <TOKEN>" \
  -d '{"date":"2026-06-21"}'

# 2. 日历热力图
curl -X POST https://warblood.online/api/stats/daily-counts \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <TOKEN>" \
  -d '{"month":"2026-06"}'

# 3. 项目进展
curl -X POST https://warblood.online/api/stats/project-progress \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <TOKEN>" \
  -d '{"month":"2026-06"}'

# 4. 考勤导出（浏览器访问 webapp → 日报管理 → 选择月份 → 导出考勤）
```
