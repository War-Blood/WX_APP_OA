## 待解决

1. **批量生成不生效**：后端已修复并部署。
   - ⚠️ **小程序需重新编译**：源码已改，但 `dist/dev/mp-weixin/` 仍为旧版。需执行 `npm run dev:mp-weixin` 后重新上传微信开发者工具。

2. **填写人（workers）为空**：✅ 已修复（nickname || user_name 回退），已部署。

3. **统计只有1条**：随问题1修复后自动解决。

4. **请假模板**：✅ 已改进 — today_work 现在格式为「请假：2026-06-25 至 2026-06-27，原因：XXX」。
   - 每条日志的 work_content = 类型名（请假/调休）
   - 每条日志的 remark = 用户填写的备注原因

## 下一步

> 重新编译小程序后测试：选请假 → 填起止日期+原因 → 确认生成多天日志 + 填写人不为空 + 内容格式正确。
