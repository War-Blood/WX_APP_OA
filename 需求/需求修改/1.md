请假填写。

## 当前问题

1. **批量生成不生效**：填了起始/结束日期，但只生成单条记录。
   - 代码逻辑已正确（前端传 leaveStartDate/leaveEndDate，后端 batchCreateLeaveLogs 循环 INSERT）
   - 疑似部署未生效：服务器可能运行旧版代码，需要确认 `report.service.js` 已更新并 `pm2 restart`

2. **填写人（workers）为空**：`batchCreateLeaveLogs` 只查 `SELECT nickname`，但大部分用户的 nickname 为 NULL。
   - 需改为 `nickname || user_name` 回退逻辑。

3. **统计只有1条**：因问题1导致只生成了单条记录，修复问题1后统计自然正确。

## 待补充

> 请确认：服务器上 `/var/www/daily-report/server/src/core/services/report.service.js` 是否包含 `batchCreateLeaveLogs` 函数？
> 如没有，需重新部署。
