# 2026-05-29 工作日志

## 核心任务: 小程序 API 对接 + 移除虚假数据

### 1. 创建基础设施 Skills
- 创建 `startup-expert-team` skill (~/.workbuddy/skills/) - 一键启动 SOLO 开发专家团
- 从 `backend/project.md` 生成 `backend-project` skill (.workbuddy/skills/) - 后端行为约束
- 从 `test/project.md` 生成 `test-project` skill (.workbuddy/skills/) - 测试行为约束

### 2. Phase 1: 需求分析（PM: 许清楚）
- 产出: `backend/docs/PRD-Api-Refactor.md`
- 识别 8 个前端页面存在假数据，后端 features/ 4 个模块为空桩

### 3. Phase 2: 技术方案（TM: 程晓技）
- 产出: `backend/docs/Tech-Plan.md`
- 12 个子任务并行策略，P0 优先聚焦 stats/review/approval/report/message

### 4. Phase 3: 并行工程实现（12个子任务）

**后端新增模块:**
- Stats 模块: features/controllers, services, routes + 路由注册 ✅
- Review 模块: features/controllers, services, routes + review_records.sql ✅

**后端改造模块:**
- Approval: controller/service 参数映射（兼容 tab/type 新旧参数）✅
- Report: submit 扩展 20+字段 + draft/delete 接口 + ALTER SQL ✅
- Message: formatMessage 字段映射对齐 ✅

**前端基础设施:**
- request.js: 移除 VITE_USE_MOCK 分支，添加 dev-mode-token 处理 ✅

**前端页面改造（全部移除硬编码数据和 setTimeout）:**
- home/index.vue: stats/activities/unread 全量 API 调用 ✅
- approval/index.vue: Tab/筛选/分页联动 API ✅
- employee/report-history.vue: Tab/分页 API ✅
- message/index.vue: 列表/Tab/markRead API ✅
- admin/review-list.vue: 列表/统计/Tab API ✅
- login/index.vue: 改用 authApi 封装 ✅

### 6. 数据库迁移 ✅（2026-05-29 16:30 完成）
- `review_records` 表 → wx_app_oa 库 ✅
- `approval_cc` 表 → wx_app_oa 库 ✅
- `daily_reports` 表扩展（17个字段 + 索引）→ daily_report 库 ✅
- 全部使用安全迁移脚本（跳过已存在的列和索引）
- 后端新增: 6 个文件（3 controllers + 3 services）+ 3 个 routes
- 后端改造: 5 个文件（controllers/services/routes）
- 数据库脚本: 3 个（review_records.sql + approval_cc.sql + alter_daily_reports.sql）
- 前端新增: 1 个（stats.js service）
- 前端改造: 7 个（home/approval/report-history/message/review-list/login/request.js）

### 7. 第二轮修复：剩余假数据清除 + 页面 API 对接（8个子任务并行）
- 首页 tasks 待办事项硬编码 → 从 statsApi 同步 ✅
- 发起审批 handleSubmit → 调用 approvalApi.create ✅
- 日报详情假数据 → 调用 reportApi.getDetail + 加载态 ✅
- 日报编辑 handleSubmit → 调用 reportApi.submit/saveDraft + 草稿加载 ✅
- 驳回重提假数据 → 调用 reportApi.getDetail/submit ✅
- 审核详情假数据 → 调用 reviewApi.getDetail/doAction + 意见输入框 ✅
- 个人中心统计硬编码 → 调用 statsApi + messageApi ✅
- review 字段名对齐 → 后端新增 user/time 映射 ✅

### 8. 前端UI优化：uni-icons统一 + 布局标准化 + 样式规范落地（三阶段共约20个子任务并行）

**P0: uni-icons 统一切换** ✅
- OaIcon 组件（9文件 ~30处）→ uni-icons ✅
- IconPark 组件 + @icon-park/vue-next（3文件）→ uni-icons ✅
- 审批类型静态PNG（2文件）→ uni-icons ✅
- 额外修复6个组件 + 1个页面未覆盖的 OaIcon 残留 ✅
- 删除废弃：oa-icon组件、icon-park组件、generated PNG、iconfont.css、@icon-park/vue-next依赖 ✅
- 禁止：14个文件修改，4个目录删除，1个npm依赖移除

**P1: 布局一致性优化** ✅
- NavBar slot 增强（向后兼容）✅
- 5个页面内联 NavBar → 共享 NavBar 组件 ✅
- 8个文件的卡片 border-radius/padding 统一标准（16rpx/24rpx）✅
- 8个文件的 content-padding 统一为 24rpx ✅

**P2: 样式规范落地** ✅
- uni.scss 追加全局 .card/.card-header/.card-title 类 ✅
- 13个页面 SCSS 变量清理（删除重复声明 + @import uni.scss）✅
- home 页面 rgba 不一致值修复 ✅
- 4个页面增加下拉刷新统一（refresher 方案）✅
